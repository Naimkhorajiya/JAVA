class Rectangle {
    int length;
    int breadth;

    Rectangle() {
        length = 5;
        breadth = 10;
    }

    void displayArea() {
        System.out.println("Rectangle Area: " + (length * breadth));
    }
}